@model KyivBarGuideDomain.Model.FavouriteBar

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>FavouriteBar</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="AddedById" class="control-label"></label>
                <select asp-for="AddedById" class="form-control" asp-items="ViewBag.AddedById"></select>
                <span asp-validation-for="AddedById" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AddedId" class="control-label"></label>
                <select asp-for="AddedId" class="form-control" asp-items="ViewBag.AddedId"></select>
                <span asp-validation-for="AddedId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
